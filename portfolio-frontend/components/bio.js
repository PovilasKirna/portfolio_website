import { Box } from "@chakra-ui/layout";
import styled from "@emotion/styled";

export const BioSection = styled(Box)`
	padding-left: 3.4em;
	text-indent: -3.4em;
`;

export const BioYear = styled.span`
	font-weight: bold;
	margin-right: 1em;
`;

export const BioBold = styled.text`
	font-weight: bold;
`;
